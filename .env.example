# Bernard team — environment (NAMES ONLY, no real values). Copy to ~/.openclaw/.env, chmod 600.
# --- LLM ---
LLM_API_KEY=
EMBEDDINGS_API_KEY=            # for memory_search vector index
# --- Telegram (one bot per agent) ---
TELEGRAM_BOT_TOKEN_BERNARD=
TELEGRAM_BOT_TOKEN_POLLY=
TELEGRAM_BOT_TOKEN_DEXTER=
# --- Gateway ---
OPENCLAW_GATEWAY_TOKEN=
# --- Git backup ---
GIT_BACKUP_TOKEN=             # a token for your PRIVATE backup repo
